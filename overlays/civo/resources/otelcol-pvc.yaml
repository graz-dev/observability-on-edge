apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otelcol-file-storage
  namespace: edge-obs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: civo-volume
  resources:
    requests:
      storage: 1Gi
