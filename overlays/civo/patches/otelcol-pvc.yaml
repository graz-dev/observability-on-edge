- op: replace
  path: /spec/template/spec/volumes
  value:
    - name: config
      configMap:
        name: otel-collector-config
    - name: file-storage
      persistentVolumeClaim:
        claimName: otelcol-file-storage
